@{
    ViewBag.Title = "ATM";
}

<div class="container">
    <h2>Операции</h2>
    <div class="form-horizontal">
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <a href="@Url.Action("Balance", "Atm")" class="btn btn-default">Баланс</a>
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <a href="@Url.Action("Cash", "Atm")" class="btn btn-default">Снять деньги</a>
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <a href="@Url.Action("Index", "Atm")" class="btn btn-default btn-danger">Вернуть карту</a>
            </div>
        </div>

    </div>
</div>
