@model ADB.Web.Models.BalanceModel

@{
    ViewBag.Title = "Balance";
}
<div class="container">
    <h2>ATM</h2>
    <div class="form-horizontal">
        <h4>Ваш баланс</h4>
        <hr/>

        <div class="form-group">
            @Html.DisplayFor(x => x.Value)
            @Html.DisplayFor(x => x.CurrencyType)
        </div>
    </div>
</div>
<div>
    @Html.ActionLink("Назад", "Menu")
</div>
<div>
    @Html.ActionLink("Вернуть карту", "Index")
</div>
